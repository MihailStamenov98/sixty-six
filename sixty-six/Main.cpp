#include<iostream>
#include "GameController.h"


using namespace std;

int main()
{
	/*GameController game;
	game.play();*/

	/*vector<Card> passed = { Card(clubs,king), Card(diamonds,jack),  Card(diamonds,ten),  Card(hearts,queen), Card(spades,king) };
	auto c = Card::getRemainingCards(passed);
	for (int i = 0; i < passed.size(); i++)
	{
		cout << "(" << passed[i].getColorName() << ", " << passed[i].getName() << "), ";
	}
	cout << endl << endl;
	for (int i = 0; i < c.size(); i++)
	{
		cout << "(" << c[i].getColorName() << ", " << c[i].getName() << "), ";
	}*/
	return 0;
}